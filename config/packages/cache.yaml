framework:
    cache:
        # Unique name of your app: used to compute stable namespaces for cache keys.
        #prefix_seed: your_vendor_name/app_name

        # En desarrollo, usar filesystem para que los cambios se reflejen inmediatamente
        # APCu puede cachear servicios y causar problemas en desarrollo
        # app: cache.adapter.apcu  # Deshabilitado en dev para evitar problemas con cambios

when@prod:
    framework:
        cache:
            # En producción, usar APCu para mejor rendimiento
            app: cache.adapter.apcu
